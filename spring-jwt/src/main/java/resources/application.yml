server:
  port: 8080
  servlet:
    context-path: /
    encoding:
      charset: UTF-8
      enabled: true
      force: true

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/test?serverTimezone=Asia/Seoul
    username: root
    password: 123qwe

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      dialect:
        org.hivernate.dialect.MySQL5InnoDBDialect
